.title!= replace(data.name, createLink)
if sortParameters == null
	- sortParameters = Object.keys(data.metadata)
	
.metadata
	ul
		each key in sortParameters
			li
				span.key= key+': '
				span.value= data.metadata[key]
				
if fulltext
	//- .found= found
	//- .keywords
	if data._kwic != null
		//- - console.log(data._kwic)
		if data._kwic['Transcription'] && data._kwic['Transcription'].length == 1
			ul.kwic
				li!= data._kwic['Transcription'][0]
		else
			each kwic, layer in data._kwic
				ol.kwic
					each row in kwic
						li!= row						

- console.log(model)

.metadata
	button.green.toggle-metadata Metadata
	.table-container
		table
			each m, i in model.get("metadata")
				tr
					td= m.field
					td= m.value

h2!= title

.container
	.facsimile
		iframe(src=model.get("facsimiles")[0].zoom)
	.text!= text